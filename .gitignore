*.pyc
__pycache__/
.pytest_cache/
*.bak

# Runtime state/secrets yarr.py writes next to itself — never part of
# the template, only ever local state on a running install.
addon_secrets.json
yarr_state.json
